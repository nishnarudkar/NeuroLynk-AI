# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# Virtual environments
venv/
.venv/
env/

# Model artifacts — committed for Render deployment
# (DVC tracks these for local/CI use; Render needs them in the repo)
# models/

# MLflow local backend
mlflow.db
mlruns/

# Data (tracked via DVC)
data/pd_speech_features.csv

# Generated static assets — committed for Render deployment
# static/feature_importance.png
# static/learning_curve.png

# Secrets — never commit
.env
.dvc/config.local

# Monitoring — keep structure but ignore generated reports and large CSVs
monitoring/drift_report.html
monitoring/drift_summary.txt
monitoring/drift_feature_details.csv
monitoring/current_data.csv

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
.kiro/

# Testing & Artifacts
.hypothesis/
Artifacts/
artifacts/